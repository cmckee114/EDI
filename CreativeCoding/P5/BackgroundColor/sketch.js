function setup() {
createCanvas(windowWidth,windowHeight);// this creates the window width and height to be the size of my browser window
background(229,251,255); //light blue background
}

function draw() {

}
